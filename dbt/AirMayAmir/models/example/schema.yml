version: 2

models:
  - name: stg
    description: "Silver model for example table"
    columns:
      - name: example_id
        description: "Unique identifier for the example record"
        tests:
          - unique
          - not_null

  - name: fact
    description: "Gold model for example aggregation"
    columns:
      - name: example_id
        description: "Aggregated identifier"
